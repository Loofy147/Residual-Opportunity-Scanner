[project]
name = "residual-opportunity-scanner"
version = "0.1.0"
description = "Evidence-first discovery and scoring of residual digital infrastructure opportunities"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
